# Liquid Customization #
In addition to the standard tags, filters, and variables available for use, the Rails Community Platform has added
a number of custom tags, filters, and drops that are specific to the platform.  For an overview of liquid see {file:LiquidOverview.md}.
Below is the list custom objects:

## Tags ##
<% tag_docs.each do |tag_doc| %>
* <%= tag_doc %>
<% end %>

## Filters ##
<% filter_docs.each do |filter_doc| %>
* <%= filter_doc %>
<% end %>

## Drops ##
<% drop_docs.each do |drop_doc| %>
* <%= drop_doc %>
<% end %>